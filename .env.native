# RoundTable — Native Build Environment
# Copy this to .env and set VITE_API_BASE_URL before running: npm run build:native
#
# This URL is embedded into the app at build time.
# It must point to your deployed Express backend (Railway, Render, Fly.io, etc.)
#
VITE_API_BASE_URL=https://api.roundtable.app
